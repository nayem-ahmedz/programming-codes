// Take four parameters. Multiply the four numbers and then return the result

function multiply(a, b, c, d){
    const mul = a * b * c * d;
    return mul;
}

const output = multiply(5, 1, 2, 10);
console.log(output);